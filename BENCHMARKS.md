# Benchmarks

Current dmt-rs throughput numbers, reproduction procedure, and
performance model live in [`docs/benchmarks.md`](docs/benchmarks.md).

Historical experiment writeups (M3 Max cross-hardware validation,
MSSQL RAM bump, MySQL target tuning, Go vs dmt-rs head-to-head) are
preserved in [`docs/benchmarks-archive.md`](docs/benchmarks-archive.md)
for reasoning and methodology — all numbers there are superseded.

Tuning rationale for the MySQL target container (the one piece of
benchmark-adjacent config that lives outside the unified doc) is in
[`docs/mysql-target-container.md`](docs/mysql-target-container.md).
